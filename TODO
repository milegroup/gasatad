ALL:
	Help
	When exporting csvs, it always adds the .csv extension (check this)
	Review axis of bar charts and patches and margins
	Change descriptive statistics and significance tests (html?)
	Problem when reading xls or xlsx files
	When there are many categories in discreet data, allow user to group them

WIN:
	Colors of cells

MAC:
	Try to remove "python" fron the main app bar
	Improve detection of new versions: now, running GASATaD from source detects new binary versions for MacOS
	Colors of cells

DEB:

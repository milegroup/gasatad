ALL:
	Scrollbars
	Many problems reading csv files:
		- Strings without quotes
		- Long file names (test1.out.csv)
		- Little information of errors
WIN:
	Build the .msi package

MAC:
	Build the package
	Try to remove "python" fron the main app bar

DEB:
	Open file opens the explorer in "/usr/share/gasatad"
	Terminal mode loading csv requires the full path of the files
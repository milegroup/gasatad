ALL:
	Help
	When exporting csvs, it always adds the .csv extension (check this)
	Problem when reading xls or xlsx files
	When network is slow, it takes a while to open
	Review buttons, menus...
	Change "Attention" to "ERROR"
	Change format of basic statistics and significance tests
	See "No data" cells in NUTRITION_WH2
	Add an option to transform columns to continous variables
	
WIN:

MAC:
	Try to remove "python" fron the main app bar
	Improve detection of new versions: now, running GASATaD from source detects new binary versions for MacOS

DEB:
